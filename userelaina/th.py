'''
`throws` `nThread` `getlock` `lock` `unlock`

    >>> throws(f:function,args=tuple())
    >>> nThread(n=16,clk=0,f=None)
'''

from userelaina._th import throws,nThread,getlock,lock,unlock
